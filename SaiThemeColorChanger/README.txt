Drag the sai2.exe file into the app to change the UI colors

This edits the file in place cause otherwise saving it as a secondary file causes weird runes to popup, but that also happens if you just copy/paste the exe so